// Simple test runner for types test

import { expect } from 'chai'
import { describe, it } from 'mocha'

describe('Basic test', () => {
  it('should work', () => {
    expect(1 + 1).to.equal(2)
  })
})
